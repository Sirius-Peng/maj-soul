- [x] 应用可在 macOS 上启动并在内置浏览器中打开雀魂页面
- [x] 应用可在 Windows 上启动并在内置浏览器中打开雀魂页面
- [x] 能自动检测到对局开始并创建新的会话目录与 `session.json`
- [x] 能自动检测到对局结束并完成导出落盘
- [x] 能识别对局类型（三麻/四麻）并写入会话与局级元信息
- [x] 关键帧机制可工作：画面变化触发识别，未变化不做重识别
- [x] `session.json` schema 覆盖：元信息、局、帧、状态快照、置信度、错误
- [x] 会话与关键帧识别结果会增量写入 SQLite，且可按会话/时间范围查询读取
- [x] 自家手牌识别在基准截图集上通过测试
- [x] 宝牌指示牌识别在基准截图集上通过测试
- [x] 四家河识别在基准截图集上通过测试（遮挡/牌背以 unknown 表示）
- [x] 剩余牌堆数：可识别时给数值；不可识别时给 null + reason
- [x] 提供一键依赖安装与测试命令，首次运行可自动拉齐依赖并跑通测试
